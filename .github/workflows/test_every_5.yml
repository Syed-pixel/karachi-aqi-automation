name: Test - Runs Every 5 Minutes

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes (1:00, 1:05, 1:10, etc.)
  workflow_dispatch:       # Manual trigger

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Show time and trigger info
      run: |
        echo "========================================"
        echo "✅ TEST WORKFLOW TRIGGERED!"
        echo "🕐 Current time: $(date)"
        echo "🎯 Trigger type: ${{ github.event_name }}"
        echo "📅 Next run: In 5 minutes"
        echo "========================================"
        echo ""
        echo "If you see this, GitHub schedules WORK!"
        echo "If not, there's a problem with GitHub Actions"
        echo ""
